type Post {
  id: ID!
  authorId: ID!
  author: Profile!
  content: String!
  imageUrl: String
  likesCount: Int!
  commentsCount: Int!
  isLiked: Boolean!
  isSaved: Boolean!
  comments: CommentConnection!
  createdAt: DateTime!
  updatedAt: DateTime!
}

